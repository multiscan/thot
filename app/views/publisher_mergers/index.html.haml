%h1 Listing publisher_mergers

%table
  %tr
    %th New
    %th Create
    %th Edit
    %th Update
    %th
    %th
    %th

  - @publisher_mergers.each do |publisher_merger|
    %tr
      %td= publisher_merger.new
      %td= publisher_merger.create
      %td= publisher_merger.edit
      %td= publisher_merger.update
      %td= link_to 'Show', publisher_merger
      %td= link_to 'Edit', edit_publisher_merger_path(publisher_merger)
      %td= link_to 'Destroy', publisher_merger, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Publisher merger', new_publisher_merger_path
